{% extends 'base.html.twig' %}

{% block title %}!{% endblock %}

{% block body %}

<div class="bg-zinc-50">
	<div class="transition flex min-h-screen items-center justify-center w-full h-full">
    <h1 class='animate-bounce hover:animate-spin font-bold text-[100px]'>{{choice}}</h1>
	</div>
</div>
{% endblock %}
